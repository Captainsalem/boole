{{#post}}
<div class="featured mx-auto max-w-7xl mt-16 p-8 rounded-lg border-2 border-gray-500 dark:border-gray-800">
    <div class="mt-0 mr-auto mb-0 ml-auto">
        {{#if comments}}
        <div class="">
            {{comments}}
        </div>
        {{/if}}
    </div>
</div>
{{/post}}
