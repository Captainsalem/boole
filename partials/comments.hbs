{{#post}}
<div class="featured mx-auto max-w-7xl mt-16 p-8 rounded-lg border border-dotted dark:border-slate-800 border-slate-400  dark:text-slate-300 hover-glow">
    <div class="mt-0 mr-auto mb-0 ml-auto">
        {{#if comments}}
        <div class="">
            {{comments}}
        </div>
        {{/if}}
    </div>
</div>
{{/post}}