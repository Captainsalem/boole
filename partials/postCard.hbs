<div class="mx-auto my-20 grid justify-center gap-4 sm:grid-cols-1 md:grid-cols-2 xl:grid-cols-3">
    {{#get "posts" limit="9" include="tags,authors"}}
    {{#foreach posts}}
    <div class="relative flex max-w-[24rem] flex-col overflow-hidden rounded-xl bg-white dark:bg-transparent border-2 border-dotted dark:border-slate-700 bg-clip-border text-gray-700 dark:text-gray-400 shadow-md hover:shadow-lg transition"
        style="height: 100%;">
        {{#if feature_image}}
        <a href="{{url}}" class="block w-full overflow-hidden border-b border-dotted border-slate-700 z-0 rounded-t-lg transition-all duration-300 hover:scale-110">
            <img src="{{img_url feature_image size="xl"}}"
                alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
                class="w-full h-[200px] object-cover" />
        </a>
        {{/if}}
        <div class="flex flex-col justify-between flex-grow pl-8 pr-8 pt-2">
            <div>
                <div class="flex flex-wrap mb-2">
                    <a href="{{primary_tag.url}}">
                        <div
                            class="flex gap-1.5 items-center justify-center text-slate-700 dark:text-inherit text-xs focus:ring-4 focus:outline-none focus:ring-blue-300 dark:focus:ring-blue-800 font-medium rounded-lg">
                            {{#if featured}}
                            {{> "icons/fire"}}
                            {{/if}}
                            <p class="text-sm">{{primary_tag.name}}</p>
                        </div>
                    </a>
                </div>
                <h4
                    class="block font-sans text-2xl antialiased font-semibold leading-snug tracking-normal text-blue-gray-900">
                    <a href="{{url}}">{{title}}</a>
                </h4>
                
                {{#if custom_excerpt}}
                <p class="block mt-3 text-sm antialiased leading-relaxed text-gray-700 dark:text-gray-400">
                    {{custom_excerpt}}
                </p>
                {{/if}}
            </div>
        </div>

        <hr class="mt-1 mb-0 border border-dotted dark:border-slate-700">
        <div class="flex items-center justify-between p-3">
            {{#if authors}}
            <div class="flex items-center -space-x-3">
                <a href="{{primary_author.url}}" title="{{primary_author.name}}" class="block">
                    <img src="{{img_url primary_author.profile_image}}" alt="avatar"
                        class="relative inline-block h-9 w-9 !rounded-full border-2 border-white object-cover object-center hover:z-10">
                </a>
            </div>
            {{/if}}
            {{#if reading_time}}
            <div class="flex gap-1 items-center">
                <span>{{> "icons/clock"}}</span>
                <span class="text-sm">{{reading_time}}</span>
            </div>
            {{/if}}
            <time class="block font-sans text-base antialiased font-normal leading-relaxed text-inherit"
                datetime="{{date format='YYYY-MM-DD'}}">
                {{date format="MMMM DD, YYYY"}}
            </time>
            <div class="flex items-center">
                <a href="{{url}}">
                <span class="hover:animate-ping">{{> "icons/arrow-up-right"}}</span>
                </a>
            </div>
        </div>
    </div>
    {{/foreach}}
    {{/get}}
</div>
