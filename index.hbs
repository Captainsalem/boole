{{!< default}}
<main class="items-center justify-center">
{{#is "home"}}
  {{> hero}}
  {{> image-slider}}
{{/is}}

<section class="max-w-7xl mx-auto mb-16 p-8">
<h2 class="text-sm font-semibold uppercase text-slate-900 dark:text-slate-400 border-slate-900 dark:border-slate-800 border-b border-dotted mb-10 pb-2.5">All Posts</h2>
    <div class="mx-auto grid justify-center gap-8 sm:grid-cols-1 md:grid-cols-2 xl:grid-cols-3">
      {{#foreach posts}}
          {{> postCard }}
      {{/foreach}}
    </div>
</section>
  {{ pagination}}
  {{> popular_tags}}
</main>








